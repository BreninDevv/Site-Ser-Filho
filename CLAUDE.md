@AGENTS.md

# Claude / Cursor

Este arquivo só aponta para `AGENTS.md` — uma única fonte de verdade para qualquer agente (Cursor, Claude Code, Codex).

Não duplique regras aqui. Se precisar mudar comportamento do projeto, edite `AGENTS.md` ou as rules em `.cursor/rules/`.
