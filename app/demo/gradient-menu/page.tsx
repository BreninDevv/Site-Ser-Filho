import { DemoOne } from "@/components/demo-one";

export const metadata = {
  title: "Demo · Gradient Menu | Ser Filho",
  robots: { index: false, follow: false },
};

export default function GradientMenuDemoPage() {
  return <DemoOne />;
}
